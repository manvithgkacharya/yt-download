
# Core Requirements
Flask==2.3.2
yt-dlp==2023.11.16
gunicorn==21.2.0

# Production Extras
gevent==23.9.1  # For better concurrent request handling
python-dotenv==1.0.0  # For environment variables

# Optional (for enhanced functionality)
ffmpeg-python==0.2.0  # If you need advanced audio/video processing
pytube==15.0.0  # Alternative YouTube library
```

### Key Features:

1. **Production-Ready**:
   - Includes Gunicorn for production WSGI server
   - Gevent for handling concurrent requests efficiently

2. **YouTube Specific**:
   - yt-dlp with recent version for best compatibility
   - Optional pytube as backup library

3. **Best Practices**:
   - Pinned versions for stability
   - Separated core vs optional requirements

### Installation:


# For basic functionality:
pip install -r requirements.txt

# For production deployment:
pip install -r requirements.txt --no-deps  # If you want just these exact packages
```

### Notes:
- The versions are current as of November 2023
- For deployment platforms (Railway, Heroku, etc.), this file will automatically install dependencies
- If you need FFmpeg for audio conversion, you'll need to install it separately on your system

